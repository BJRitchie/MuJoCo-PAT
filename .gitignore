build/
install/
log/
*.o
*.a
*.so
*.so.*
CMakeCache.txt
CMakeFiles/
__pycache__/
*.pyc
.vscode/
.idea/
*.swp
.DS_Store
*.bag
*.db3
outputs/

# clangd: generated compile DB is a build artifact; .clangd config is checked in
/compile_commands.json
.cache/

# docker: base compose + .env are committed; local machine tweaks (GPU, extra
# mounts, ports) go in an override that is not.
docker-compose.override.yml
