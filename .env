# Read automatically by docker compose. The dev container runs as this
# uid:gid so bind-mounted files keep your ownership. Change if `id -u` /
# `id -g` on your machine isn't 1000.
UID=1000
GID=1000
# colcon / make parallelism inside the container. Raise on a roomy machine.
BUILD_JOBS=2
